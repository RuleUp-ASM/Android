# compileSdk 37 을 그대로 두면 Robolectric 이 지원하지 않는 android-all jar 를 찾다 실패한다.
# 이 판(4.16)이 지원하는 SDK 로 모듈 전체를 고정한다 — 테스트마다 @Config 를 붙이지 않아도 되도록.
sdk=35

# 기본 창(320dp)에서는 assertIsDisplayed() 가 "규칙이 맞는가"가 아니라 "화면 밖으로 밀렸는가"를 재게 된다.
# 흔한 폰 크기를 못 박아 그 축을 없앤다.
qualifiers=w411dp-h891dp-xhdpi
