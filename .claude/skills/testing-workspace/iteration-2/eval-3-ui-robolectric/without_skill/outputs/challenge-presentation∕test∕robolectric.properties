# 34 는 Robolectric 이 안정적으로 내려받는 android-all 이다. compileSdk(37) 를 따라갈 이유가 없다 —
# 화면 로직에 SDK 분기가 없고, 최신 SDK 를 쓰면 android-all 이 없을 때 전 테스트가 통째로 깨진다.
sdk=34

# 기본 화면(320dp)은 하단 CTA·멤버 섹션이 화면 밖으로 밀려 "안 보임"이 오탐이 된다. 실제 폰 크기로 그린다.
qualifiers=w411dp-h891dp-xhdpi
