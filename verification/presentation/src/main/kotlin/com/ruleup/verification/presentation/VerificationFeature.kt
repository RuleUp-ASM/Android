package com.ruleup.verification.presentation

/**
 * 인증 feature 프레젠테이션 모듈 앵커.
 *
 * Phase 5(화면 렌더)에서 진행률(홈) · 검증결과/실패 상세 화면(MVI + Compose)이 이 모듈에 추가된다.
 * (지금은 모듈 스캐폴딩만 존재 — 명세 부록 A Phase 5 참고)
 */
internal object VerificationFeature
